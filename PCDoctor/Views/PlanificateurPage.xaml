<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PCDoctor.Views.PlanificateurPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:PCDoctor.ViewModels">

    <Page.DataContext>
        <vm:PlanificateurViewModel/>
    </Page.DataContext>

    <Grid Margin="40" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Planificateur de taches" FontSize="32" FontWeight="SemiBold"/>

        <!-- Barre d'actions -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10">
            <Button Content="Analyser" Command="{Binding ScanCommand}"/>
            <Button Content="Activer la selection"
                    Command="{Binding EnableCommand}"
                    IsEnabled="{Binding HasScanned}"/>
            <Button Content="Desactiver la selection"
                    Command="{Binding DisableCommand}"
                    IsEnabled="{Binding HasScanned}"/>
            <Button Content="Supprimer la selection"
                    Command="{Binding DeleteCommand}"
                    IsEnabled="{Binding HasScanned}"
                    Style="{StaticResource AccentButtonStyle}"/>
            <ProgressRing IsActive="{Binding IsLoading}" Width="24" Height="24" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Liste des taches -->
        <ScrollViewer Grid.Row="2">
            <ItemsControl ItemsSource="{Binding Tasks}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                CornerRadius="8" Padding="14" Margin="0,0,0,8"
                                BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0"
                                          IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                          VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Path}" Opacity="0.6" FontSize="11"/>
                                    <TextBlock Text="{Binding Author}" Opacity="0.6" FontSize="11"/>
                                </StackPanel>
                                <Border Grid.Column="2" CornerRadius="4" Padding="8,2"
                                        VerticalAlignment="Center" Margin="12,0,0,0"
                                        Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                                    <TextBlock Text="{Binding State}" FontSize="12"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock Grid.Row="3" Text="{Binding StatusText}" Opacity="0.8" FontStyle="Italic"/>
    </Grid>
</Page>
