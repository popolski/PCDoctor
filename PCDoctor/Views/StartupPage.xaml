<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PCDoctor.Views.StartupPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:PCDoctor.ViewModels">
    <Page.DataContext>
        <vm:StartupViewModel/>
    </Page.DataContext>

    <Grid Margin="40" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Programmes au démarrage" FontSize="32" FontWeight="SemiBold"/>
        <Button Grid.Row="1" Content="🔄 Actualiser" Command="{Binding LoadCommand}" HorizontalAlignment="Left"/>

        <ScrollViewer Grid.Row="2">
            <ItemsControl ItemsSource="{Binding Entries}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,8"
                                BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Command}" Opacity="0.6" FontSize="12" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Location}" Opacity="0.5" FontSize="11"/>
                                </StackPanel>
                                <TextBlock Grid.Column="1" Text="{Binding Status}" VerticalAlignment="Center" Margin="0,0,16,0" Opacity="0.8"/>
                                <Button Grid.Column="2" Content="Activer/Désactiver" VerticalAlignment="Center"
                                        Command="{Binding DataContext.ToggleCommand, ElementName=PageRoot}"
                                        CommandParameter="{Binding}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock Grid.Row="3" Text="{Binding StatusText}" Opacity="0.8" FontStyle="Italic"/>
    </Grid>
</Page>