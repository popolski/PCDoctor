<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PCDoctor.Views.BloatwarePage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:PCDoctor.ViewModels">
    <Page.DataContext>
        <vm:BloatwareViewModel/>
    </Page.DataContext>

    <Grid Margin="40" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Bloatware Windows" FontSize="32" FontWeight="SemiBold"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10">
            <Button Content="🔍 Analyser" Command="{Binding ScanCommand}"/>
            <Button Content="🗑️ Désinstaller la sélection" Command="{Binding RemoveCommand}"
                    IsEnabled="{Binding HasScanned}" Style="{StaticResource AccentButtonStyle}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2">
            <ItemsControl ItemsSource="{Binding Apps}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="14" Margin="0,0,0,8"
                                BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Display}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding PackageName}" Opacity="0.6" FontSize="11"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock Grid.Row="3" Text="{Binding StatusText}" Opacity="0.8" FontStyle="Italic"/>

        <!-- Section : Nettoyage complémentaire -->
        <Border Grid.Row="4"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="20"
                BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <StackPanel Spacing="12">
                <TextBlock Text="Nettoyage complémentaire" FontWeight="SemiBold" FontSize="16"/>
                <TextBlock Text="Actions destructrices sur des composants système. Confirmer avant de lancer."
                           Opacity="0.65" TextWrapping="Wrap" FontSize="12"/>
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <Button Content="Supprimer OneDrive"
                            Command="{Binding RemoveOneDriveCommand}"/>
                    <Button Content="Bloquer raccourcis Edge"
                            Command="{Binding DisableEdgeShortcutsCommand}"/>
                </StackPanel>
                <TextBlock Text="{Binding ExtraStatusText}" Opacity="0.8" FontStyle="Italic" FontSize="12"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>